import { InputRestrictionDirectiveDirective } from './input-restriction-directive.directive';

describe('InputRestrictionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRestrictionDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
